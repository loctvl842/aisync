[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "aisync-cli"
version = "0.1.0"
description = ""
authors = ["loctvl842 <loclepnvx@gmail.com>"]
# readme = "README.md"
packages = [{ include = "aisync_cli" }]

[tool.poetry.dependencies]
python = "^3.12"
aisync = { path = "../core", develop = true }
click = "^8.1.7"

[tool.poetry.scripts]
aisync = "aisync_cli.cli:main"
